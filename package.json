{
  "name": "odin-book",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "node server/server.js",
    "build": "npm install --include=dev --prefix client && npm run build --prefix client && npm install --prefix server && npx prisma generate --schema server/prisma/schema.prisma && npx prisma migrate deploy --schema server/prisma/schema.prisma"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
